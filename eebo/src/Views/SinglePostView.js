
/**
 * rendered to show a single post item
 * @param {} param0 
 * @returns 
 */
export function SinglePostView({}) {
    return (
        <section id="signlePost">
            <h2>design post here</h2>
        </section>
    );
}